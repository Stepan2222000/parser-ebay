# Анализ производительности воркера parser_ebay

Документ фиксирует исследование скорости парсинга PDP/каталога eBay:
где узкое горло, как влияют CPU / сеть / прокси / фильтрация трафика, и
какое железо брать под боевых воркеров. Все цифры — живые замеры
2026-06-13 на реальных eBay-страницах.

## 1. Вопрос и метод

Вопрос: почему PDP парсится медленно (на тестовом сервере было 17–30с) и
что это ускорит.

Инструменты замера:
- **`StageProfiler`** в `ebaylib` — поэтапная разбивка времени одной задачи:
  `swap` (замена страницы) · `nav` (навигация/загрузка) · `ready`
  (готовность DOM/селекторов) · `desc` (iframe-описание item) · `parse`
  (разбор HTML) · `queue`/`write` (БД). Сумма этапов = время задачи.
- **CPU-бенч** — одноядерный Python-цикл (20M итераций) для сравнения мощи
  процессоров без браузера.
- **`curl`** к eBay — сырой сетевой замер (TTFB/ping) без браузера и CPU.
- **Параллельный харнесс** — N воркеров одновременно, замер агрегатного
  темпа и насыщения CPU/RAM.

Природа этапов: `nav` и `desc` — **сеть** (загрузка ресурсов eBay);
`ready` и `parse` — **CPU** (исполнение JS и разбор HTML).

## 2. Главный вывод: скорость PDP упирается в одноядерный CPU

Сравнение четырёх машин (одни и те же item'ы, headless=False, прогретая
сессия, медиана steady-state):

| Машина | CPU-бенч 20M | vCPU | direct | proxy без кэша | proxy + кэш |
|---|---|---|---|---|---|
| **MacBook (Apple M5)**, домашняя сеть | 0.96с | 10 | 2.3с | 8.3с | 2.4с |
| Старый сервер (Xeon E5-4669 v4) | 6.33с | 2 | 19с | 23с | 16.5с |
| Ryzen 9 5950X (тариф DE-2) | 1.60с | 2 | 3.6с | 3.5с | 4.2с |
| **Ryzen 9 9950X (тариф HI-LOAD-3)** | 1.06с | 4 | 3.2с | 3.6с | **2.7с** |

Ключевой факт: **PDP линейно по одноядерной мощи CPU.** Старый Xeon (бенч
6.33с) → PDP 19с; Ryzen 9950X (бенч 1.06с) → PDP 2.7с. Разница ровно в CPU.

Доказательство, что сеть ни при чём: сырой `curl` сервер→eBay даёт TTFB
**0.1с**, ping **5мс** (быстрее, чем у Мака) — то есть сеть у датацентр-сервера
отличная, а тормозил именно процессор (исполнение тяжёлого JS страницы eBay
+ C++-патчи cloakbrowser на слабом ядре).

Поэтапно (direct, Мак vs старый Xeon):

| этап | Мак | старый Xeon | природа |
|---|---|---|---|
| nav | 1.8с | 5.4с | сеть |
| **ready** | 0.1с | **8.1с** | **CPU (исполнение JS)** |
| desc | 0.5с | 2.8с | сеть |
| parse | 0.1с | 2.1с | CPU |

`ready` (готовность страницы) раздувается с 0.1с до 8с — это и есть слабый
CPU, который медленно исполняет JS до появления селекторов.

## 3. Фильтрация трафика: рычаг цены, а на прокси — и скорости

`ebay_filtering` (CacheClient): блокирует картинки/шрифты/медиа + отдаёт
JS/CSS-бандлы из общего кэша в Postgres-«мозге». Замеры:

- **Трафик −88…99%.** На сервере с кэшем реально скачано **1–3 МБ** на
  прогон против ~175 МБ; ~600 cache hits, ~700 запросов заблокировано,
  saved ~180 МБ.
- **На прямом быстром канале скорость не меняет** (Мак: 2.3с без кэша → 2.4с
  с кэшем) — парсинг и так не ждёт картинки.
- **На прокси-пути ВОЗВРАЩАЕТ скорость.** Мак через ShopProxy: без кэша 8.3с
  (`nav` 8.7с — латентность прокси), с кэшем **2.4с** (`nav` 2.0с). Кэш
  срезал `nav`, потому что ~99% байтов (бандлы + блок картинок) **минуют
  прокси** — через прокси идёт лишь уникальный HTML товара.

Вывод: фильтрацию подключать **вместе с прокси** — двойная выгода: режет
оплачиваемый трафик прокси в ноль И снимает латентность прокси.

Особенность: кэш «learn-only» на первом прогоне (снимок грузится при старте
один раз) — HIT'ы и экономия идут со второго воркера/прогона; несколько
воркеров делят кэш через Postgres автоматически.

## 4. Прокси: тип почти не важен (с кэшем), скорость не чинит

| Прокси (9950X, с кэшем) | median | nav | примечание |
|---|---|---|---|
| ShopProxy (статичный US datacenter) | 2.7с | 1.6с | стабильный |
| residential DataImpulse (ротация) | 3.2с | 1.7с | шумнее, `desc` скачет |

С кэшем `nav` одинаков (кэш убрал почти весь прокси-трафик), тип прокси по
скорости почти не различается. На хорошо подключённом датацентр-сервере
прокси добавляет к скорости **почти ноль** (9950X: direct 3.2с ≈ proxy 3.6с).
Жуткие +6с прокси на Маке — это была слабая домашняя сеть до прокси, а не сам
прокси.

Прокси нужны для **обхода блоков** (свежий IP против AccessDenied) и
**правильной вёрстки** (US-гео → ebay.com/USD; не-US IP рискует редиректом на
ebay.de и кривыми селекторами), а **не для скорости**.

## 5. Где упор сейчас: на хорошем железе — в СЕТЬ

Боевая связка (9950X + ShopProxy + кэш), типичный PDP ~2.5с:

| этап | время | доля | природа |
|---|---|---|---|
| **nav** | 1.6с | ~64% | 🌐 сеть (загрузка уникального HTML товара) |
| ready | 0.6с | ~24% | ⚙️ CPU |
| parse | 0.1с | ~5% | ⚙️ CPU |
| desc | 0.1–0.3с | ~7% | 🌐 сеть |

Переключение произошло: на современном 4-ядерном CPU мы **больше не
CPU-bound**. Доминирует `nav` (~64%) — это сеть: загрузка уникальной страницы
товара (её не закэшировать) + бот-сенсор eBay. Это **пол ≈ 2.5с/PDP**, и
ускорить его нечем — ни CPU, ни ядрами, ни прокси.

## 6. Сколько воркеров на сервер (9950X, 4 vCPU, 7.3 ГБ)

Параллельный замер (ShopProxy + кэш):

| N воркеров | per-PDP | агрегат | PDP/час | load (4 ядра) | стабильность |
|---|---|---|---|---|---|
| 1 | 2.7с | 0.37/с | ~1330 | ~2 | ✅ |
| **2** | **4.0с** | **0.50/с** | **~1800** | 4.5 | ✅ |
| 3 | 6.4с | 0.47/с | ~1700 | 9.1 | ⚠️ CPU впритык |
| 4 | 6.8с | 0.59/с | ~2120 | 8.8 | ❌ кэш-конфликт |

Темп сервера упирается в потолок **~0.5–0.6 PDP/с (~1800–2100 PDP/час)** уже
на 2 воркерах — потолок задаёт 4 vCPU, не число воркеров. RAM не предел (пик
5.7 ГБ из 7.3, ~1.3 ГБ/воркер).

**Рекомендация: 2 воркера на 4-vCPU сервер для постоянной работы** — ~90% от
потолка темпа, CPU с запасом (стабильно сутками), без кэш-конфликтов. 3–4
воркера дают +10–15% темпа ценой ×1.7 латентности и нестабильности.

Правило масштабирования: **~1 воркер на 2 vCPU** современного Ryzen. Один
такой сервер ≈ 1800 PDP/час. Дальше масштаб — числом серверов (единица
мощности = контейнер, SPEC §7, §10).

## 7. Рецепт боевого воркера

- **Сервер:** современный CPU (Ryzen 9000/7000-класс), **4+ vCPU**, хорошая
  датацентр-сеть. Старые Xeon E5 не брать (CPU-bound на 17с). Ориентир —
  ~2.7с/PDP на 9950X.
- **Прокси:** статичный US datacenter (ShopProxy-класс) — против блоков и за
  US-вёрстку; по скорости на хорошем сервере ≈ бесплатно.
- **Фильтрация:** включена всегда — режет трафик прокси на 90–99% и снимает
  латентность прокси.
- **Плотность:** 2 воркера на 4-vCPU сервер; масштаб — числом серверов.
- **Целевой темп:** ~2.7с/PDP на воркера, ~1800 PDP/час на сервер.

Влияние на SPEC §10 (интеграция с server_logic): `parse_ms` обратно
пропорционален одноядерной мощи воркер-сервера — это нужно закладывать в NCU;
единица мощности — воркер (~1.3 ГБ, ~2 vCPU/воркер по факту).

## 8. Открытые вопросы (зоны других репозиториев)

- **`ebay_filtering`:** под ≥4 одновременных воркеров, учащих один бандл,
  вылезает `asyncpg InternalServerError: tuple concurrently updated` — конфликт
  записи в «мозг». При 2 воркерах не воспроизводится. Зона ebay_filtering.
- **Прокси:** датацентровый `151.247.125.253:50100` привязан к IP сервера
  (whitelist) — работает с сервера, не с произвольной машины. Резидентный
  DataImpulse — ротация суб-ресурсов, медленнее и нестабильнее статичного.
- **Вёрстка:** не-US exit-IP рискует редиректом eBay на локальный домен →
  US-прокси обязателен для воркеров вне США.
